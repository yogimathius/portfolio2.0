import React from 'react';
import PortfolioList from './PortfolioList';

const index = (props) => {
  console.log(props);
  return (
    <div>
     <PortfolioList /> 
    </div>
  );
};

export default index;